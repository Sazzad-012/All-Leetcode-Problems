class Solution {
public:
    bool isSubsequence(string s, string t) {
        int n = s.size(), m = t.size();
        int i = 0;  
        for (int j = 0; i < n && j < m; j++) {
            if (s[i] == t[j]) {
                i++;
            }
        }

        return i == n;  
    }
};
